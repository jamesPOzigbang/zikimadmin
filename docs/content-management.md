# PRD: 리포트 컨텐츠 관리 시스템 (Content Management System)

**문서 버전:** 1.0  
**최종 수정일:** 2025-12-08  
**담당자:** 지킴진단 프로덕트팀  
**관련 파일:** `content-management-standalone.html`

---

## 1. 제품 개요 (Product Overview)

### 1.1 목적 (Purpose)
관리자가 리포트에 표시되는 진단 항목별 안내 문구, 정의, 특약 사항을 거래 유형(매매/전세/월세) 및 건물 유형(집합/일반)에 따라 독립적으로 관리할 수 있는 CMS(Content Management System)를 제공합니다.

### 1.2 비즈니스 가치 (Business Value)
- 리포트 품질 향상: 정확하고 맞춤형 가이드 제공
- 운영 효율성: 코드 수정 없이 컨텐츠 업데이트 가능
- 법적 리스크 감소: 특약 사항을 체계적으로 관리하여 누락 방지
- 확장성: 새로운 진단 항목 추가가 용이함

### 1.3 타겟 사용자 (Target Users)
- **주 사용자:** 지킴진단 컨텐츠 관리자 (법무팀, 운영팀)
- **간접 수혜자:** 고객 (정확한 가이드를 제공받음)

---

## 2. 사용자 스토리 (User Stories)

### US-011: 거래 유형별 컨텐츠 관리
**As a** 컨텐츠 관리자  
**I want to** 매매, 전세, 월세 각각에 대해 다른 안내 문구를 설정하고  
**So that** 거래 유형별 특성에 맞는 정보를 제공할 수 있다

**수용 기준:**
- [ ] 6개 탭(매매-집합, 매매-일반, 전세-집합, 전세-일반, 월세-집합, 월세-일반)이 제공됨
- [ ] 탭 전환 시 해당 탭의 21개 진단 항목이 독립적으로 로드됨
- [ ] 탭 전환에는 Fade-in 애니메이션 적용
- [ ] 현재 활성 탭은 녹색 배경으로 강조됨

### US-012: 진단 항목별 상태 관리
**As a** 법무팀 담당자  
**I want to** 각 진단 항목에 대해 "양호" 상태와 "확인 필요" 상태의 가이드를 별도로 작성하고  
**So that** 리포트가 자동으로 적절한 문구를 표시할 수 있다

**수용 기준:**
- [ ] 각 진단 항목 카드에 [양호]/[확인 필요] 토글 버튼이 제공됨
- [ ] 토글 전환 시 해당 상태의 입력 폼이 표시됨
- [ ] 양호는 녹색, 확인 필요는 주황색 강조
- [ ] 두 상태의 데이터는 독립적으로 저장됨

### US-013: 컨텐츠 유형 구분 (안내/설명/특약)
**As a** 운영팀 관리자  
**I want to** "안내", "설명", "특약" 3가지 컨텐츠 유형을 선택하여 각각 다른 내용을 입력하고  
**So that** 리포트에서 상황에 맞는 정보를 제공할 수 있다

**수용 기준:**
- [ ] 상태 선택 후 라디오 버튼으로 [안내]/[설명]/[특약] 선택 가능
- [ ] 선택된 유형의 입력 폼만 화면에 표시됨
- [ ] 기본값은 "안내"로 설정됨
- [ ] 각 유형별 placeholder 텍스트가 다름

### US-014: 동적 텍스트 블록 추가/삭제
**As a** 컨텐츠 관리자  
**I want to** 하나의 컨텐츠 유형 내에서 여러 개의 텍스트 블록을 추가하거나 삭제하고  
**So that** 복잡한 설명을 구조화하여 작성할 수 있다

**수용 기준:**
- [ ] "+ 추가하기" 버튼 클릭 시 새로운 입력 블록 생성
- [ ] 각 블록은 제목(2행) + 내용(다행) textarea로 구성
- [ ] 추가된 블록에는 "삭제" 버튼이 표시됨
- [ ] 첫 번째 기본 블록은 삭제 불가
- [ ] 블록 개수 제한 없음

### US-015: 컨텐츠 저장 및 타임스탬프 기록
**As a** 컨텐츠 관리자  
**I want to** 작성한 컨텐츠를 저장하고 최종 수정 시간을 확인하고  
**So that** 언제 업데이트되었는지 추적하고 버전 관리를 할 수 있다

**수용 기준:**
- [ ] "💾 저장하기" 버튼 클릭 시 데이터 저장
- [ ] 저장 성공 시 "저장되었습니다" Alert 표시
- [ ] 각 항목 카드 상단에 "최종 수정: YYYY.MM.DD HH:MM" 표시
- [ ] 저장 시 타임스탬프가 자동 업데이트됨
- [ ] 저장 실패 시 에러 메시지 표시

### US-016: 토지별도등기 특수 처리
**As a** 법무팀 담당자  
**I want to** "토지별도등기" 항목의 "확인 필요" 상태에서 세부 유형(구분지상권/근저당권/기타)을 선택하고  
**So that** 더 정밀한 가이드를 제공할 수 있다

**수용 기준:**
- [ ] "토지별도등기" 항목의 "확인 필요" 상태에서만 드롭다운 표시
- [ ] 드롭다운 옵션: 구분지상권, 근저당권, 기타
- [ ] 선택 시 `updateLandType()` 함수 호출
- [ ] 선택된 유형이 데이터와 함께 저장됨

---

## 3. 기능 요구사항 (Functional Requirements)

### FR-008: 탭 기반 컨테이너 시스템
**우선순위:** P0 (Critical)  
**설명:** 6개 탭을 클릭하여 해당 탭의 진단 항목 그리드를 표시합니다.

**세부 사항:**
- `openTab(event, tabName)` 함수로 탭 전환 처리
- 모든 `.tab-content`의 `display: none` 설정 후 선택된 탭만 `display: block`
- 선택된 탭 버튼에 `.active` 클래스 추가 (녹색 배경)
- CSS Transition을 통한 부드러운 전환 효과

**기술 구현:**
```javascript
function openTab(evt, tabName) {
    document.querySelectorAll('.tab-content').forEach(tc => {
        tc.style.display = 'none';
        tc.classList.remove('active');
    });
    
    document.querySelectorAll('.tab-btn').forEach(btn => {
        btn.classList.remove('active');
    });
    
    document.getElementById(tabName).style.display = 'block';
    setTimeout(() => {
        document.getElementById(tabName).classList.add('active');
    }, 10);
    
    evt.currentTarget.classList.add('active');
}
```

### FR-009: 동적 진단 항목 렌더링
**우선순위:** P0 (Critical)  
**설명:** 21개 항목을 순회하며 각각의 편집 가능한 카드 UI를 생성합니다.

**세부 사항:**
- `renderChecklist(containerId)` 함수가 각 탭마다 호출됨
- `checklistItems` 배열을 `forEach`로 순회
- 각 항목에 고유 ID 부여: `${containerId}-${index}`
- 동적으로 생성된 HTML을 `innerHTML`로 주입

**기술 구현:**
```javascript
function renderChecklist(containerId) {
    const container = document.getElementById(containerId);
    let html = '<div class="checklist-grid">';
    
    checklistItems.forEach((item, index) => {
        const uniqueId = `${containerId}-${index}`;
        html += `
            <div class="checklist-item" id="item-${uniqueId}">
                <div class="checklist-header">
                    <label class="checklist-label">${item}</label>
                    <span class="update-date" id="date-${uniqueId}">
                        최종 수정: ${formatDate(new Date())}
                    </span>
                </div>
                
                <div class="status-tabs-wrapper">
                    <button class="status-tab safe active" 
                            onclick="toggleStatusItem('${uniqueId}', 'safe')">
                        양호
                    </button>
                    <button class="status-tab caution" 
                            onclick="toggleStatusItem('${uniqueId}', 'caution')">
                        확인 필요
                    </button>
                </div>
                
                <div id="${uniqueId}-safe" class="status-content-area active">
                    ${renderRadioGroup(uniqueId, 'safe', item)}
                </div>
                
                <div id="${uniqueId}-caution" class="status-content-area">
                    ${renderRadioGroup(uniqueId, 'caution', item)}
                </div>
            </div>
        `;
    });
    
    html += '</div>';
    container.innerHTML = html;
}
```

### FR-010: 상태/유형 전환 로직
**우선순위:** P0 (Critical)  
**설명:** 사용자가 상태 또는 컨텐츠 유형을 변경할 때 UI를 동적으로 업데이트합니다.

**함수 목록:**
1. `toggleStatusItem(uniqueId, status)`: [양호] ↔ [확인 필요] 전환
   - 상태 탭 버튼의 `.active` 클래스 조작
   - 해당 상태의 `.status-content-area` 표시
   
2. `toggleContentSection(uniqueId, status, type)`: [안내] ↔ [설명] ↔ [특약] 전환
   - 라디오 버튼 상태에 따라 `.content-input-area` 표시/숨김

### FR-011: 동적 입력 필드 관리
**우선순위:** P1 (High)  
**설명:** 텍스트 블록을 동적으로 추가하거나 삭제합니다.

**함수:**
```javascript
function createInputBlock(placeholder, isRemovable = false) {
    const removeBtn = isRemovable ? 
        `<button class="btn-remove-field" 
                 onclick="this.closest('.input-wrapper-dynamic').remove()">
            삭제
         </button>` : '';
    
    return `
        <div class="input-wrapper-dynamic">
            <div class="input-header">
                <textarea class="title-input" 
                          placeholder="제목을 입력하세요" 
                          rows="2"></textarea>
                ${removeBtn}
            </div>
            <textarea class="checklist-input" 
                      placeholder="${placeholder}" 
                      rows="3"></textarea>
        </div>
    `;
}

function addInputField(containerId, placeholder) {
    const container = document.getElementById(containerId);
    container.insertAdjacentHTML('beforeend', 
                                  createInputBlock(placeholder, true));
}
```

### FR-012: 데이터 저장 및 타임스탬프 업데이트
**우선순위:** P0 (Critical)  
**설명:** 사용자가 입력한 컨텐츠를 저장하고 수정 시간을 기록합니다.

**기술 구현:**
```javascript
function saveContent(uniqueId, status, type) {
    // 1. 현재 시간 획득
    const now = new Date();
    
    // 2. DOM에서 입력값 수집
    const container = document.getElementById(`list-${uniqueId}-${status}-${type}`);
    const blocks = container.querySelectorAll('.input-wrapper-dynamic');
    const contentData = Array.from(blocks).map(block => ({
        title: block.querySelector('.title-input').value,
        content: block.querySelector('.checklist-input').value
    }));
    
    // 3. LocalStorage 또는 API로 저장 (향후 구현)
    const dataKey = `content_${uniqueId}_${status}_${type}`;
    localStorage.setItem(dataKey, JSON.stringify(contentData));
    
    // 4. 타임스탬프 업데이트
    const itemElement = document.getElementById(`item-${uniqueId}`);
    itemElement.setAttribute('data-last-updated', now.toISOString());
    
    const dateElement = document.getElementById(`date-${uniqueId}`);
    dateElement.textContent = `최종 수정: ${formatDate(now)}`;
    
    // 5. 성공 피드백
    alert(`저장되었습니다.\n항목: ${uniqueId}\n상태: ${getStatusName(status)}\n타입: ${type}`);
}

function formatDate(date) {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');
    const hours = String(date.getHours()).padStart(2, '0');
    const minutes = String(date.getMinutes()).padStart(2, '0');
    return `${year}.${month}.${day} ${hours}:${minutes}`;
}
```

---

## 4. 비기능 요구사항 (Non-functional Requirements)

### NFR-009: 확장성
- 새로운 진단 항목 추가 시 `checklistItems` 배열에만 추가하면 자동 렌더링
- 새로운 거래 유형 추가 시 탭만 추가하면 기존 로직 재사용 가능

### NFR-010: 데이터 무결성
- 저장 전 입력값 Validation (빈 값 체크)
- LocalStorage 용량 제한 확인 (5MB)
- 저장 실패 시 자동 재시도 또는 에러 로깅

### NFR-011: 사용성
- 탭 전환 시 애니메이션 지연 10ms로 부드러운 UX 제공
- 입력 필드 Focus 시 border 색상 변경으로 현재 위치 시각화
- 라디오 버튼에 커스텀 스타일 적용 (체크 시 Pop 애니메이션)

### NFR-012: 브라우저 호환성
- Chrome 90+, Safari 14+, Firefox 88+ 지원
- LocalStorage API 지원 브라우저 필수

---

## 5. 데이터 모델 (Data Model)

### 컨텐츠 데이터 구조
```typescript
interface ContentItem {
    tabType: string;        // 'sale-multi' | 'sale-general' | ...
    checklistIndex: number; // 0-20 (21개 항목 인덱스)
    status: 'safe' | 'caution';
    contentType: 'guide' | 'definition' | 'special';
    blocks: ContentBlock[];
    lastUpdated: string;    // ISO 8601 format
    landType?: string;      // 토지별도등기 전용 ('구분지상권' | '근저당권' | '기타')
}

interface ContentBlock {
    title: string;          // 제목 (2줄)
    content: string;        // 내용 (여러 줄)
}

// LocalStorage 키 형식
// "content_{tabType}-{index}_{status}_{type}"
// 예: "content_sale-multi-0_safe_guide"
```

### 진단 항목 리스트
```javascript
const checklistItems = [
    "위반건축물", "대지권", "토지별도등기", "가등기", "압류/가압류",
    "처분금지가처분", "신탁", "경매개시결정", "근저당권", "임차권등기명령",
    "전세권", "민간임대주택등록 여부", "등기사항 변경예정", "건물,토지 소유자 일치",
    "임대사업자 등록", "보증금미반환 이력", "기존채무금액", "여유금액",
    "보증보험 예비심사", "보증금 대출 예비 심사", "최우선 변제권"
];
```

---

## 6. UI/UX 명세 (UI/UX Specifications)

### 6.1 탭 네비게이션
```html
<div class="tabs-header">
    <button class="tab-btn active">매매-집합건물</button>
    <button class="tab-btn">매매-일반건물</button>
    <button class="tab-btn">전세-집합건물</button>
    <button class="tab-btn">전세-일반건물</button>
    <button class="tab-btn">월세-집합건물</button>
    <button class="tab-btn">월세-일반건물</button>
</div>
```
**스타일:**
- 기본: 흰색 배경, 회색 테두리, Pill 모양 (border-radius: 9999px)
- Active: 녹색 배경(`--primary-color`), 흰색 텍스트, 그림자

### 6.2 진단 항목 카드 구조
```
┌─────────────────────────────────┐
│ [위반건축물]      [최종수정시간] │
├─────────────────────────────────┤
│ [양호(Active)] [확인필요]        │  ← 상태 탭
├─────────────────────────────────┤
│ ○ 안내 (checked)                │
│ ○ 설명                          │  ← 컨텐츠 유형 라디오
│ ○ 특약                          │
│ [드롭다운] (토지별도등기만)      │
├─────────────────────────────────┤
│ ┌─────────────────────┐         │
│ │ 제목 [textarea 2행]  │         │
│ └─────────────────────┘         │
│ ┌─────────────────────┐         │
│ │ 내용 [textarea 3행]  │         │
│ └─────────────────────┘         │
├─────────────────────────────────┤
│ [+ 추가하기] [💾 저장하기]       │
└─────────────────────────────────┘
```

### 6.3 인터랙션 패턴

#### 상태 전환
1. 사용자가 [확인 필요] 버튼 클릭
2. `toggleStatusItem('sale-multi-0', 'caution')` 호출
3. [양호] 버튼의 `.active` 제거, [확인 필요] 버튼에 `.active` 추가
4. `#sale-multi-0-safe` 영역 숨김, `#sale-multi-0-caution` 영역 표시
5. Fade-in 애니메이션 효과 (0.3초)

#### 블록 추가
1. 사용자가 [+ 추가하기] 버튼 클릭
2. `addInputField('list-sale-multi-0-safe-guide', placeholder)` 호출
3. `createInputBlock(placeholder, true)` 함수로 HTML 생성
4. `insertAdjacentHTML('beforeend', html)`로 DOM 끝에 삽입
5. 새 블록이 Fade-in으로 등장

#### 블록 삭제
1. 사용자가 [삭제] 버튼 클릭
2. Inline event: `this.closest('.input-wrapper-dynamic').remove()`
3. 해당 블록 DOM 요소만 제거
4. 다른 블록들은 영향 없음

---

## 7. 사용자 플로우 (User Flow)

### 플로우 4: 새로운 안내 문구 작성
```
[컨텐츠 관리 페이지 진입]
    ↓
[탭 선택: 전세-집합건물]
    ↓
[진단 항목 선택: "위반건축물" 카드]
    ↓
[상태 선택: 확인 필요]
    ↓
[컨텐츠 유형 선택: 안내 (라디오 버튼)]
    ↓
[기본 블록에 내용 입력]
    ├─ 제목: "위반건축물 발견 시 조치사항"
    └─ 내용: "해당 매물은 위반건축물로 확인되었습니다..."
    ↓
[+ 추가하기 클릭하여 2번째 블록 생성]
    ├─ 제목: "위반건축물의 법적 정의"
    └─ 내용: "건축법 제110조에 따르면..."
    ↓
[💾 저장하기 클릭]
    ↓
[저장 성공 Alert 확인]
    ↓
[최종 수정 시간이 현재 시간으로 업데이트됨]
```

### 플로우 5: 토지별도등기 세부 유형 설정
```
[컨텐츠 관리 페이지 진입]
    ↓
[탭: 매매-집합건물 선택]
    ↓
[진단 항목: "토지별도등기" 카드]
    ↓
[상태: 확인 필요 선택]
    ↓
[드롭다운 자동 표시]
    ↓
[드롭다운에서 "구분지상권" 선택]
    ↓
[updateLandType() 함수 호출]
    ├─ Console에 선택 로그 출력
    └─ 선택값을 데이터와 함께 저장 준비
    ↓
[컨텐츠 유형: 특약 선택]
    ↓
[특약 문구 작성: "본 계약 시 구분지상권에 대한..."]
    ↓
[저장 완료]
```

---

## 8. 예외 처리 및 엣지 케이스 (Edge Cases)

### EC-009: 빈 값 저장 시도
**시나리오:** 사용자가 아무 내용도 입력하지 않고 저장  
**처리:**
1. `saveContent()` 함수 내에서 모든 textarea 값 검증
2. 빈 블록이 있으면 해당 필드에 빨간색 border 표시
3. Alert: "내용을 입력해주세요"
4. 저장 중단

### EC-010: LocalStorage 용량 초과
**시나리오:** 데이터가 5MB를 초과하여 저장 실패  
**처리:**
1. `try-catch`로 `localStorage.setItem()` 감싸기
2. QuotaExceededError 발생 시
3. Alert: "저장 공간이 부족합니다. 이전 데이터를 삭제하거나 관리자에게 문의하세요"
4. 콘솔에 에러 로그 기록

### EC-011: 탭 전환 시 미저장 데이터
**시나리오:** 사용자가 컨텐츠를 입력하고 저장하지 않은 채 다른 탭으로 이동  
**처리:**
1. Dirty Flag 설정 (입력 필드 변경 시 `hasUnsavedChanges = true`)
2. 탭 전환 시 Dirty Flag 확인
3. `confirm()`: "저장하지 않은 내용이 있습니다. 이동하시겠습니까?"
4. 취소 시 탭 전환 중단, 확인 시 진행

### EC-012: 중복 저장 방지
**시나리오:** 사용자가 연속으로 저장 버튼을 빠르게 클릭  
**처리:**
1. 저장 중 플래그 설정 (`isSaving = true`)
2. 저장 중일 때는 버튼 비활성화
3. 저장 완료 후 플래그 해제
4. 버튼 재활성화

---

## 9. 스타일 시스템 (Style System)

### 색상 팔레트
```css
:root {
    --primary-color: #009720;    /* 메인 그린 */
    --primary-light: #009720;
    --accent-color: #009720;
    --bg-color: #F8FAFC;         /* 배경 */
    --card-bg: #FFFFFF;
    --text-primary: #1E293B;
    --text-secondary: #64748B;
    --border-color: #E2E8F0;
    
    /* 상태 색상 */
    --status-success-bg: #DCFCE7;
    --status-success-text: #166534;
    --status-error-bg: #FEF3C7;
    --status-error-text: #92400E;
}
```

### 애니메이션
```css
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes radioPop {
    0% {
        transform: translate(-50%, -50%) scale(0);
    }
    100% {
        transform: translate(-50%, -50%) scale(1);
    }
}
```

---

## 10. API 명세 (API Specification) - 향후 구현

### POST /api/content/save
**설명:** 컨텐츠 저장

**Request:**
```json
{
    "tabType": "sale-multi",
    "checklistIndex": 0,
    "status": "safe",
    "contentType": "guide",
    "blocks": [
        {
            "title": "제목",
            "content": "내용"
        }
    ],
    "landType": null
}
```

**Response:**
```json
{
    "success": true,
    "contentId": "uuid-here",
    "lastUpdated": "2023-12-08T14:30:00Z"
}
```

### GET /api/content/load
**설명:** 저장된 컨텐츠 불러오기

**Query Parameters:**
- `tabType`: string
- `checklistIndex`: number
- `status`: string
- `contentType`: string

**Response:**
```json
{
    "success": true,
    "data": {
        "blocks": [...],
        "lastUpdated": "2023-12-08T14:30:00Z",
        "landType": "구분지상권"
    }
}
```

---

## 11. 테스트 시나리오 (Test Scenarios)

### TS-001: 기본 CRUD 작업
1. 탭 선택 → 진단 항목 카드 렌더링 확인
2. 상태 전환 → UI 변경 확인
3. 컨텐츠 유형 선택 → 해당 폼 표시 확인
4. 텍스트 입력 → 저장 → 타임스탬프 업데이트 확인
5. 페이지 새로고침 → 저장된 데이터 로드 확인

### TS-002: 동적 블록 조작
1. [+ 추가하기] 클릭 → 새 블록 생성 확인
2. 2번째 블록에 [삭제] 버튼 존재 확인
3. [삭제] 클릭 → 해당 블록만 제거 확인
4. 첫 번째 블록은 삭제 버튼 없음 확인

### TS-003: 토지별도등기 특수 케이스
1. "토지별도등기" + "양호" 선택 → 드롭다운 미표시 확인
2. "확인 필요"로 전환 → 드롭다운 자동 표시 확인
3. 드롭다운에서 "근저당권" 선택 → updateLandType() 호출 확인

---

## 12. 향후 개선사항 (Future Enhancements)

### Phase 2
- [ ] 백엔드 API 연동 (LocalStorage → Database)
- [ ] 버전 관리 시스템 (컨텐츠 변경 이력 추적)
- [ ] 컨텐츠 미리보기 기능 (실제 리포트 형태로 렌더링)
- [ ] 일괄 복사/붙여넣기 (한 탭의 내용을 다른 탭으로)

### Phase 3
- [ ] AI 기반 컨텐츠 추천 (과거 데이터 분석)
- [ ] 다국어 지원 (영어, 중국어 버전)
- [ ] 역할 기반 접근 제어 (RBAC)
- [ ] 컨텐츠 승인 워크플로우 (작성자 → 검수자 → 승인자)

### Phase 4
- [ ] Rich Text Editor 도입 (굵게, 밑줄, 링크 등)
- [ ] 이미지 업로드 지원
- [ ] 템플릿 시스템 (자주 사용하는 문구를 템플릿으로 저장)
